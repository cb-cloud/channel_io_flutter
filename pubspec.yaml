name: channel_io_flutter
description: A new flutter plugin project.
version: 0.0.20
author:
homepage:

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.cbcloud.channel_io_flutter
        pluginClass: ChannelIoFlutterPlugin
      ios:
        pluginClass: ChannelIoFlutterPlugin

